apiVersion: apps/v1
kind: Deployment
metadata:
  name: sm-subs
  labels:
    app: sm-subs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sm-subs
  template:
    metadata:
      labels:
        app: sm-subs
    spec:
      containers:
      - env:
        - name: "DB_USER"
          value: "postgres"
        - name: "DB_PASSWORD"
          value: "asdf1234!"
        - name: "DB_HOST"
          value: "sm-db"
        - name: "DB_PORT"
          value: "5432"
        - name: "DB_NAME"
          value: "subs-service"
        - name: "JWT_SECRET_KEY"
          value: "mJMLk2qwEFKp1Lx2FatzwVOA6-3FjMqkLEAWu74uCU9"
        name: sm-subs
        image: devwikirepo/sm-subs:1.0.0
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: sm-subs
  labels:
    app: sm-subs
spec:
  selector:
    app: sm-subs
  ports:
  - port: 80
    targetPort: 5000
  type: ClusterIP
